services:
  backend:
    build: .
    container_name: ipnext_backend
    restart: unless-stopped
    ports:
      - "${SERVER_PORT:-8288}:8288"
    env_file:
      - .env
    extra_hosts:
      - "host.docker.internal:host-gateway"
